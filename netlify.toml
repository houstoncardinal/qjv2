[build]
  command = "npm run build"
  publish = "dist"

[build.environment]
  NODE_VERSION = "22"
  NPM_FLAGS = "--legacy-peer-deps"
  # Build the TanStack Start server for Netlify Functions instead of the
  # default Cloudflare target.
  NITRO_PRESET = "netlify"
